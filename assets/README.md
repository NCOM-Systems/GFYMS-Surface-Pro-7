# GFYMS project assets

Place the official GFYMS project logo here as:

`assets/gfyms-logo.png`

Also keep a scalable source version such as:

`assets/gfyms-logo.svg`

The README, GitHub social preview, ISO branding and KDE integration should use the same canonical asset.

The repository currently does not contain the user's existing logo, so the exact personal logo should be uploaded rather than regenerated or replaced.